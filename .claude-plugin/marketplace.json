{
  "name": "claude-code-speckit-plugin",
  "owner": {
    "name": "DgxSparkLabs"
  },
  "metadata": {
    "description": "Provides `init` and `upgrade` skills to install, run, and upgrade GitHub Spec Kit (spec-driven development) via the `specify` CLI."
  },
  "plugins": [
    {
      "name": "speckit",
      "source": "./",
      "description": "Provides `init` and `upgrade` skills to install, run, and upgrade GitHub Spec Kit (spec-driven development) via the `specify` CLI."
    }
  ]
}
